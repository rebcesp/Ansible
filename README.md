## Como Ejecutar las tareas?

`ansible-playbook create_users.yml `

### Agregar las llaves publicas con privilegios de root en Authorized Keys $HOME/.ssh/authorized_key

`ansible-playbook pubskey.yml`
